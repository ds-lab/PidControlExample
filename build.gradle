version '0.0.0'

apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
    jcenter()
}

dependencies {
    compile 'org.jfree:jfreechart:1.0.+'

    testCompile group: 'junit', name: 'junit', version: '4.+'
}
